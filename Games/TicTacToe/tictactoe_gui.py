import pygame

class TicTacToe:
    def __init__(self, window, clock, pixelratio,validChars,client, username):
        self.window = window

        self.clock = clock
        self.pixelratio = pixelratio